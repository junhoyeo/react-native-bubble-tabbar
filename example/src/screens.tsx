import * as React from 'react';
import styled from 'styled-components/native';

export const DummyScreen: React.FC = () => <Container />;

const Container = styled.View`
  flex: 1;
  background-color: purple;
`;
